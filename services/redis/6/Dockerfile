FROM redis:6

# Copy Redis configuration if needed
# COPY config/redis.conf /usr/local/etc/redis/redis.conf

# Expose Redis port
EXPOSE 6379

# Use default Redis configuration
# Or use custom config: CMD ["redis-server", "/usr/local/etc/redis/redis.conf"]
CMD ["redis-server"]

